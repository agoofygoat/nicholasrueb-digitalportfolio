# TODO — UI redesign (glassy white + green trims)

-  ] Update `styles.css` token system (fix undefined tokens like --primary/--accent/--surface/--border/--muted) and normalize color usage.
- [ ] Restyle background + section/panel surfaces to be mostly white glass with green-tinted accents.
- [ ] Convert hero right card from dark gradient to light glass; add layered CSS-only unique graphics (blobs/grid/dots/vignette).
- [ ] Improve buttons, cards, filters with consistent green hover/focus rings and better shadows.
- [ ] Add reduced-motion handling and ensure animations still trigger.
- [ ] Minimal `index.html` tweak only if needed for background ornaments (otherwise keep structure).
- [ ] Manual test: open `index.html` in browser; verify nav + filter interactions, no broken styling.
